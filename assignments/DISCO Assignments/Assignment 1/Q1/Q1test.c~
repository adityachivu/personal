#include <stdio.h>
int main(){
	int i=0,m=0,n=0,t=0,k=0;
	int a[101];
	for(i=0;i<101;i++){a[i]=0;}
	for(i=0;i<100;i++){
		scanf("%d %d",&m,&n);
		a[n]=m;
	}scanf("%d",&t);
	while(t--){
		scanf("%d",&k);
		if(t!=0)
			printf("%d\n",a[k]);
		else
			printf("%d",a[k]);
	}return 0;
}
